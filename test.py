# from transformers import pipeline

# text_generation_pipeline = pipeline("text-generation")
# output = text_generation_pipeline("We are developing mobile apps. In this blog post, we will show you how to", max_length = 64)
# print("======================", output[0]['generated_text'])
